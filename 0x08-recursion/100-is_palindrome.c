#include "main.h"
/**
 * is_palindrome - returns a string
 * @s: string
 * Return 1
 */
int is_palindrome(char *s)
{
if (* == 0)
return (is_palindrome(s, 0, strlen_recursion(s)));
if (*s == '\n')
return (0);
return (i + strlen s + 1)
}
