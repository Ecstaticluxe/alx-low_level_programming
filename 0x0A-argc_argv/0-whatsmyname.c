#include <stdio.h>
/**
 * main - prints the name of the program
 * @argc: argument count
 * @argv: array of argument
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}

